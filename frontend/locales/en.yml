en:
  plugins:
    browse_pages:
      label: Update Browse Pages
      actions:
        update: Update
      messages:
        updating_records: Update browse page info
  job:
    update_browse_pages_job: Update Browse Pages (do not use)